# Web (Vite). In dev use '' or '/api' to hit Vite proxy; in prod use full API URL.
# DummyJSON is used as demo API: https://dummyjson.com
VITE_API_BASE_URL=
VITE_APP_ENV=development

# Mobile (Expo). Same conceptual values as web.
# In dev, use the DummyJSON URL directly as mobile doesn't use proxy
EXPO_PUBLIC_API_URL=https://dummyjson.com
EXPO_PUBLIC_APP_ENV=development
